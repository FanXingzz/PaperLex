## 5. 本机模型使用说明（Windows，从安装开始）

PaperLex 是阅读软件，Ollama 是运行模型的程序，qwen2.5:7b 是需要另行下载的模型。只填写模型名称不会安装或启动它。联网翻译不需要完成本节；本节沿用软件当前配置的模型作为示例，不代表它最适合所有电脑。

### 第一步：安装并启动 Ollama

1. 打开 [Ollama Windows 官方下载页](https://ollama.com/download/windows)，下载并安装。
2. 安装后从 Windows 开始菜单打开 Ollama，让它保持后台运行。再重新打开 PowerShell（Windows 搜索框输入 PowerShell 即可）。
3. 输入下面命令并按回车；能看到版本号表示命令可用。如果提示找不到命令，先重开终端，仍不行再检查安装。[Windows 官方说明](https://docs.ollama.com/windows)

```text
ollama --version
```

### 第二步：下载并确认模型

首次下载需要联网，等待进度结束。预留模型磁盘空间；翻译速度取决于内存、显卡和模型大小。输入：

```text
ollama pull qwen2.5:7b
ollama ls
```

第二条命令列出已下载模型，确认出现 qwen2.5:7b。只有下载成功后才继续。可以先用一条示例句测试并预热模型：

```text
ollama run qwen2.5:7b "Translate into Chinese: The plasma density increases."
```

出现中文回答，表示服务和该模型都能工作。首次载入可能比后续慢；测试完成可以关闭聊天终端，但保持 Ollama 后台程序运行。[官方命令说明](https://docs.ollama.com/cli)、[示例模型页面](https://ollama.com/library/qwen2.5:7b)

### 第三步：填写 PaperLex 设置

打开左下角“设置与数据”，逐项填写并保存：

| 设置项 | 本机模型应填写的值 |
| --- | --- |
| 启用模型增强释义与双语概要 | 勾选 |
| 服务地址 | http://127.0.0.1:11434/v1 |
| 模型名称 | qwen2.5:7b，必须与 ollama ls 中的名称完全一致 |
| 模型 API Key | 本机 Ollama 留空；以前填写过远程密钥时先清除 |
| 启用联网翻译与科研语料检索 | 纯本机翻译可关闭；需要联网查论文或 MyMemory 时开启 |
| OpenAlex API Key | 与本机模型无关 |

11434 是模型服务，3089 是 PaperLex 网页，不能互换。服务地址保留 /v1，不要添加 /chat/completions，软件会补上。这个本地接口地址依据 [Ollama 官方兼容接口说明](https://docs.ollama.com/api/openai-compatibility)。

### 第四步：在论文里使用

1. 先查询一句短句，检查能否得到模型解释。
2. 打开“全文翻译与编辑”，在“翻译来源”选择“GPT / 已配置模型”。
3. 本机初次使用先把“同时翻译”设为 1 段，再点“开始／继续全文翻译”。运行稳定后可以尝试 2 段；不保证并行一定更快。
4. 已完成段落会保留。单段出错可选中它，再点“重试所选失败段”。
5. 下载完成并使用本地模型时，可以关闭软件的联网开关进行本机翻译。重新开机后先启动 Ollama，再启动 PaperLex，不必每次重新下载模型。

### 第五步：遇到问题按这里检查

| 提示或现象 | 处理方法 |
| --- | --- |
| ECONNREFUSED、fetch failed、无法连接模型 | 先打开 Ollama；确认端口为 11434。该提示表示服务地址拒绝连接 |
| Ollama 已装好，但服务未启动 | PowerShell 输入 ollama serve，保持该窗口运行；若已经由后台程序启动，不必重复启动 |
| 地址已被使用／端口占用 | 不要反复启动；先运行 ollama ls，能列出模型通常表示已有服务在运行 |
| 模型不存在／model not found | 用 ollama ls 核对，名称不同就改 PaperLex 设置；未下载就执行第二步 |
| 下载失败 | 检查网络与磁盘空间；下载阶段尚不能开始本机翻译 |
| 首段很慢／超时 | 先执行上面的单句预热，选 1 段并行，关闭占用显存的应用；仍慢可换适合电脑的小模型并同步改名称 |
| 内存或显存不足 | 当前模型超出可用资源，需要更小模型，或使用联网翻译 |
| 已关闭网络却调用失败 | 确认是本地下载的模型，服务地址为 127.0.0.1，不是云端模型或远程地址 |

全文模型每次请求最多等待 90 秒；一般查词模型请求约 45 秒。PaperLex 不会自动安装模型，也没有实际测量你电脑运行 qwen2.5:7b 的速度。启动和模型管理命令参见 [Ollama CLI 官方文档](https://docs.ollama.com/cli)。

### 不想运行本机模型：直接联网翻译

在设置开启“启用联网翻译与科研语料检索”并保存。回到全文翻译窗口，在“翻译来源”选择 **MyMemory 联网翻译 · 无需模型**，点击开始／继续。即使全局模型增强仍开启，这次全文翻译也不会调用本机模型。

MyMemory 不需要模型密钥，但会收到待译论文文字，有免费额度和服务限流，科研译法需要校对。若有远程 AI 服务，在设置填写对应服务地址、模型名和密钥，再选择“已配置模型”。切换来源前先暂停并等待在途请求结束。已完成译文不会自动覆盖。
