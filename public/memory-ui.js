import {memoryState,schedule} from '/memory.mjs';
export function memoryPanel(cards,target){
 const c=cards.find(c=>c.lastReviewed)||cards[0],m=c?memoryState(c):null,horizon=m?Math.max(1,Math.min(365,m.stability*10)):7;
 const points=Array.from({length:61},(_,i)=>{const t=i/60*horizon;return `${40+i*5},${140-110*Math.exp(Math.log(.9)*t/(m?.stability||1))}`;}).join(' ');
 return `<section class="memory-panel"><div><h3>遗忘曲线与复习目标</h3><label>希望复习前仍记得的概率 <select id="retentionTarget">${[.8,.85,.9,.95,.97].map(t=>`<option value="${t}" ${Math.abs(t-target)<.001?'selected':''}>${Math.round(t*100)}%${t===.9?'（默认）':''}</option>`).join('')}</select></label><p>目标越高，复习越频繁。更改后会重新计算已学词的到期时间。</p>${m?`<p>示例词条：<b id="curveWord"></b> · 当前预计记忆率 ${(m.retention*100).toFixed(0)}%${m.estimated?'（初始状态估算）':''}</p>`:'<p>收藏词汇后，曲线将根据你的复习记录更新。</p>'}<p class="hint">曲线根据你的评分与复习历史调整，用于估计合适的复习时间。它不代表个人记忆率的实测结果。</p></div><svg viewBox="0 0 370 175" role="img" aria-label="预计记忆率随时间下降的遗忘曲线"><path d="M40 25V140H345" fill="none" stroke="#c9c4d6"/><line x1="40" x2="340" y1="${140-target*110}" y2="${140-target*110}" stroke="#b6975c" stroke-dasharray="5 4"/><polyline points="${points}" fill="none" stroke="#7961b1" stroke-width="3"/><text x="3" y="33">100%</text><text x="13" y="144">0%</text><text x="40" y="163">复习后 0 天</text><text x="270" y="163">${horizon.toFixed(1)} 天</text></svg></section>`;
}
export function ratingLabel(card,rating,target){const days=(schedule(card,rating,Date.now(),target).due-Date.now())/86400000;return days<1/24?`${Math.round(days*1440)} 分钟后`:days<1?`${(days*24).toFixed(1)} 小时后`:`${days.toFixed(1)} 天后`;}
