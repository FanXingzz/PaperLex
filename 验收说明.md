# 验收说明

验证日期：2026-09-16。运行环境：Windows，Node.js 24.15.0，本机 Chrome。

最新改进：13 项自动测试通过。真实浏览器验证了文本/PDF 缩放、右栏字号、跨页面及刷新后的继续阅读、双语多义项与音标显示、项目术语校正，以及优化文字提取后保留批注。双栏阅读顺序和文字样式不改变原文字符顺序有独立测试。

联网复验：OpenAlex 返回 12 篇相关结果；完整句子 `The electric field increases during the discharge.` 在项目译法 electric field → 瞬态电场约束下返回“放电过程中瞬态电场增加。”；导入后中文概要生成成功。模型增强使用模拟兼容接口验证上下文与 JSON 输出契约，没有在用户电脑安装模型或完成真实模型效果评测。朗读按钮与音标界面已验证，但无人工听音验收；实际声音取决于系统英文语音或词典音频是否可用。

本次更新另验收：6 项自动测试通过（新增从外部工作目录实际执行 Windows BAT、中文输出、重复启动识别与教程入口）；三页测试 PDF 在文本精读 / PDF 原版下均可切换翻页与连续滚动，切换保持页码，滚动更新页码，第二页批注正确保存为第二页。README 已扩展为分步骤教程，同步生成可离线双击打开的 HTML 版本。未连接或安装用户本机模型。

- 5 项自动化测试通过：词形与短语匹配、句子边界、复习调度、原文摘要摘录、端到端数据持久化。
- 浏览器流程通过：新建项目 → PDF / TXT 导入 → 查询释义 → 收藏 → 选词批注与高亮 → PDF 原版画布与可选择文字层 → 跨文献例句 → 翻卡评分 → 到期队列更新。
- 实测窗口宽度 1440 和 760 像素，未发生横向页面溢出；检查了工作台与阅读页截图。没有浏览器脚本异常。
- 实际联网调用通过：MyMemory 将 electric field 翻译为“电场”；OpenAlex 项目语境检索返回 12 篇结果；联网模式导入后自动生成英文概要及中文译文。
- 校验重复文献拦截、无效批注位置拦截、未到期词条重复评分拦截、跨站请求拦截和服务重启后数据保留。

浏览器验收使用程序生成的简单 PDF 和测试文本，不是对复杂多栏论文、扫描件、全部字体及所有语言的兼容保证。真实模型接口尚未在本机连接模型验收；该功能需要用户配置可用的兼容服务。联网服务是否持续可用取决于网络与提供方额度。

测试资料保存在临时目录，验收后自动清理，没有混入正式文献库。`test-results/` 保留界面截图，仅用于检查界面，截图内容为测试文献。

运行核心验证：`npm test`。

运行浏览器验证：`node tests/browser-smoke.mjs`（脚本使用 Windows 默认 Chrome 路径）。可选设置环境变量 `LIVE_NETWORK=1` 以同时执行真实联网验收。
