# 验证说明

## 自动化检查

- `npm test`：32 项检查通过，覆盖核心检索、科研释义、句子边界、PDF 双栏分段与高亮锚点、遗忘曲线、译稿持久化、并发与错误恢复。
- DeepL 请求参数、请求去重、缓存与配额错误使用模拟响应验证；模型翻译使用模拟服务验证。
- 密钥不回传、不导出；更换模型服务器不会保留旧服务器密钥。
- 测试使用隔离临时数据，不修改用户数据库。

## 浏览器回归

`node tests/browser-smoke.mjs` 验证 PDF/TXT 导入、PDF 直接选词与批注、单下划线、收藏位置跳转、完整语境、连续滚动与翻页、缩放、右栏字号、复习、全文翻译、人工校对持久化、导出及顶部折叠。

脚本使用 Windows 默认安装位置的 Google Chrome；其他环境需调整浏览器路径。测试输出保留在本机 test-results，不提交仓库。

## 验证边界

- GPT 与 DeepL 未使用个人密钥实测付费调用；可在设置保存自己的密钥后使用连接测试。
- 机器翻译质量、专业术语准确性和复杂论文版面需要人工核对。
- 系统朗读是否可用取决于已安装语音；未进行人工听音评价。
- 外部检索服务与翻译服务的网络、额度和认证状态不由本地测试保证。
